namespace :bardshorts do
  desc "update weather data on dailyweather models"
  #rake bardshorts:fetch_todays_weather_prediction --trace
  task :test_me => :environment do
    puts "whazaup!"
  end
end
